<div class="container d-flex justify-content-center">
    <form method="post" action="{{ path('app_booking_delete', {'id': booking.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer ce rendez-vous ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.id) }}">
        <button class="btn bg-danger text-light">Supprimer</button>
    </form>
</div>